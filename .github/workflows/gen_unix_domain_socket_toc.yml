name: gen_unix_domain_socket_toc
on:
  push:
    paths:
      - '**.md'
jobs:
  generateTOC:
    name: gen_unix_domain_socket_toc
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
